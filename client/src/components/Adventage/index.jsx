import React from "react";

const Adventage = ({ src }) => {
  return (
    <video width={476} height={740} controls autoPlay>
      <source
        src={res.attributes.video.data[0].attributes.url}
        type="video/mp4"
      />
    </video>
  );
};

export default Adventage;
